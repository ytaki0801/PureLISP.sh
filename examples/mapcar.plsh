(def mapcar
  (lambda (f x)
     (cond ((eq x nil) nil)
           (t (cons (f (car x))
                    (mapcar f (cdr x)))))))

(def vs '((hoge . 10) (hage . 20) (hige . 30)))

(mapcar car vs)

(mapcar cdr vs)

exit

